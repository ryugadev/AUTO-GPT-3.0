# Env file cho upi-qr-bot — copy → /etc/upi-qr-bot.env, fill values, chmod 600
# Mỗi dòng = 1 env var; procd auto-export.

TELEGRAM_TOKEN=123456:REPLACE_ME_WITH_BOTFATHER_TOKEN
ALLOWED_USERS=
# Telegram user_id của admin. Bắt buộc để dùng /notify, /ban, /unban, /banlist.
# 0 = tắt nhóm lệnh admin. Lấy id qua @userinfobot.
ADMIN_CHAT_ID=0
MAX_CONCURRENT=100
MAX_PER_USER=10
QUEUE_CAPACITY=50
APPROVE_RETRIES=110
RESTART_THRESHOLD=20
MAX_RESTARTS=3
PROXY_POOL=
PROXY_FROM_STEP=3
USER_COOLDOWN_SECONDS=5
USER_MSG_RATE_PER_MIN=20
JOB_TIMEOUT_SECONDS=1800
DB_PATH=/overlay/upi-bot/state.db
QR_OUT_DIR=/tmp/upi-qr
BUNDLES_CACHE_DIR=/tmp/upi-bot-bundles
HTTP_TIMEOUT=30
RUST_LOG=info
